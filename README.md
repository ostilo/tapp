Kotlin Spin Wheel + React Native Wrapper

This repository contains an interview implementation of a **config-driven spin wheel UI component** written in Kotlin and exposed to React Native, plus a minimal demo app and docs.

Kotlin Spin Wheel + React Native Wrapper
# tapp
